name: job-dev-test
on:
    push:
        branches:
            - "sparkplug0025/*" # todo: remove it before merging
    schedule:
        - cron: "0 * * * *" # every hour
jobs:
    cron:
        runs-on: ubuntu-latest
        steps:
            - name: Test Job
              run: |
                  curl -i --fail-with-body --request POST \
                  --url 'https://eden-on-eos-pqhreob6f-clarionos.vercel.app/api/jobs/test' \
                  --header 'x-job-key: ${{ secrets.DEV_JOBS_AUTH_GC }}-fake'
